# Новые функции административной панели

## Обзор

В проект bi-analytics добавлены следующие улучшения:

1. **Настройки путей к файлам данных**
2. **Просмотр логов действий пользователей**
3. **Принудительное обновление отчетов**
4. **Управление правами доступа к проектам**
5. **Изменение уровня прав пользователя**

## Детальное описание

### 1. Настройки путей к файлам данных

**Расположение:** Вкладка "🔧 Настройки системы" в административной панели

**Функционал:**
- Настройка пути к файлам финансовых данных
- Настройка пути к файлам план-факт данных
- Настройка пути к файлам данных по ресурсам
- Просмотр текущих настроек с информацией о том, кто и когда их обновил

**Модуль:** `settings.py`

**Таблица БД:** `file_paths_settings`

### 2. Просмотр логов действий пользователей

**Расположение:** Вкладка "📝 Логи действий" в административной панели

**Функционал:**
- Просмотр всех действий пользователей
- Фильтрация по пользователю
- Фильтрация по типу действия
- Настройка количества отображаемых записей
- Экспорт логов в CSV

**Модуль:** `logger.py`

**Таблица БД:** `user_activity_logs`

**Логируемые действия:**
- Вход в систему (`login`)
- Выход из системы (`logout`)
- Создание пользователя (`create_user`)
- Изменение роли (`change_role`)
- Обновление настроек (`update_settings`)
- Принудительное обновление отчетов (`force_update_reports`)
- Выдача прав доступа к проекту (`grant_project_access`)
- Отзыв прав доступа к проекту (`revoke_project_access`)

### 3. Принудительное обновление отчетов

**Расположение:** Вкладка "🔄 Обновление отчетов" в административной панели

**Функционал:**
- Кнопка принудительного обновления отчетов
- Кнопка очистки кэша данных
- Отображение времени последнего обновления
- Просмотр используемых путей к файлам

### 4. Управление правами доступа к проектам

**Расположение:** Вкладка "🔐 Права доступа к проектам" в административной панели

**Функционал:**
- Выдача прав доступа пользователю к определенному проекту
- Отзыв прав доступа
- Просмотр всех текущих прав доступа, сгруппированных по проектам
- Информация о том, кто и когда выдал права

**Модуль:** `permissions.py`

**Таблица БД:** `project_permissions`

### 5. Изменение уровня прав пользователя

**Расположение:** Вкладка "👥 Управление пользователями" в административной панели

**Функционал:**
- Изменение роли пользователя (Администратор, Менеджер, Аналитик, Суперадминистратор)
- Все изменения логируются

## Структура базы данных

### Новые таблицы

1. **file_paths_settings**
   - `id` - первичный ключ
   - `setting_key` - ключ настройки (уникальный)
   - `setting_value` - значение настройки
   - `description` - описание настройки
   - `updated_at` - время обновления
   - `updated_by` - пользователь, который обновил

2. **user_activity_logs**
   - `id` - первичный ключ
   - `username` - имя пользователя
   - `action` - тип действия
   - `details` - детали действия
   - `ip_address` - IP адрес (опционально)
   - `created_at` - время действия

3. **project_permissions**
   - `id` - первичный ключ
   - `user_id` - ID пользователя (внешний ключ)
   - `project_name` - название проекта
   - `created_at` - время выдачи прав
   - `created_by` - пользователь, который выдал права
   - Уникальный индекс на (user_id, project_name)

## Новые модули

1. **logger.py** - модуль для логирования действий пользователей
2. **settings.py** - модуль для работы с настройками системы
3. **permissions.py** - модуль для работы с правами доступа к проектам

## Использование

### Логирование действий

```python
from logger import log_action

log_action(username, 'action_type', 'Детали действия')
```

### Работа с настройками

```python
from settings import get_setting, set_setting

# Получить настройку
path = get_setting('finance_files_path', '/default/path')

# Установить настройку
set_setting('finance_files_path', '/new/path', 'Описание', 'admin')
```

### Работа с правами доступа

```python
from permissions import grant_project_access, revoke_project_access, has_project_access

# Выдать права
grant_project_access(user_id, 'Проект 1', 'admin')

# Проверить права
if has_project_access(user_id, 'Проект 1'):
    # Пользователь имеет доступ
    pass

# Отозвать права
revoke_project_access(user_id, 'Проект 1')
```

## Доступ

Все новые функции доступны только пользователям с правами администратора или суперадминистратора.

## Примечания

- Все действия администраторов логируются автоматически
- Настройки путей к файлам сохраняются в базе данных
- Права доступа к проектам можно выдавать и отзывать динамически
- Логи можно экспортировать в CSV для дальнейшего анализа

---

## 📝 Дополнительные функции (обновление)

### 6. Управление фильтрами по умолчанию

**Расположение:** Вкладка "🔍 Фильтры по умолчанию" в административной панели

**Функционал:**
- Настройка фильтров по умолчанию для каждой роли и каждого отчета
- Настройка фильтров отдельно для каждого отчета
- Копирование фильтров между ролями (для одного отчета или всех отчетов)
- Просмотр всех настроенных фильтров
- Удаление фильтров

**Модуль:** `filters.py`

**Таблица БД:** `default_filters`

**Типы фильтров:**
- Текст (string)
- Число (number)
- Дата (date)
- Выбор из списка (select)
- Множественный выбор (multiselect)
- Да/Нет (boolean)

### 7. Редактирование параметров отчетов аналитиком

**Расположение:** Страница "Параметры отчетов" (доступна аналитикам и администраторам)

**Функционал:**
- Редактирование параметров для конкретных отчетов
- Выбор задачи для расчета план-факта окончания проекта
- Внесение корректировок в план бюджета
- Внесение корректировок в график проекта
- Просмотр истории изменений параметров

**Модуль:** `report_params.py`

**Таблица БД:** `report_parameters`

**Доступные параметры:**
- **Прогнозный бюджет:**
  - Задача для расчета план-факта окончания проекта
  - Корректировка плана бюджета
  - Корректировка графика проекта (дни)
- **Отклонение текущего срока от базового плана:**
  - Задача для расчета план-факта

**Страница:** `pages/analyst_params.py`

## Использование фильтров по умолчанию

Фильтры по умолчанию автоматически применяются при открытии отчета, если они настроены для роли пользователя.

## Использование параметров отчетов

Параметры отчетов используются в логике дашбордов для настройки расчетов и отображения данных.

